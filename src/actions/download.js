// import { fireDownloadArtwork, fireDownloadAudio } from "./products";

// // export const downloadArtwork = fireDownloadArtwork(p.artworkFilePath).then(url => {
// //   return url;
// // });
// // export const downloadAudio = fireDownloadAudio(p.audioFilePath).then(url => {
// //   return url
// // });

// export const downloadArtwork = url => {
//   fireDownloadArtwork(url).then(url => {
//     const newUrl = url;
//     console.log(newUrl);

//     return newUrl;
//   });
// };

// export const downloadAudio = url => {
//   fireDownloadAudio(url).then(url => {
//     return url;
//     //   console.log(url);
//   });
// };
